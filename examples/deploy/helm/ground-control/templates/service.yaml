apiVersion: v1
kind: Service
metadata:
  name: {{ include "ground-control.fullname" . }}
  labels:
    {{- include "ground-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ground-control.selectorLabels" . | nindent 4 }}
