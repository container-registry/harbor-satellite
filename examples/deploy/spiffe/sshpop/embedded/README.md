# SSH PoP - Embedded SPIRE (Phase 2)

Embedded SPIRE mode runs the SPIRE server/agent within the GC and satellite binaries directly, removing the need for separate SPIRE containers.

This deployment mode is planned for Phase 2.

For now, use the [external SPIRE setup](../README.md).
