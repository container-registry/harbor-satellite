FROM golang:1.24-alpine

WORKDIR /app

RUN apk add --no-cache git

# install air (Go 1.24 compatible, correct module path)
RUN go install github.com/air-verse/air@v1.61.7

# cache deps
COPY go.mod go.sum ./
RUN go mod download

EXPOSE 9090

CMD ["air"]
